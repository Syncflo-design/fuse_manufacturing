[project]
name = "fuse_manufacturing"
authors = [{ name = "Syncflo", email = "ops@syncflo.co.za" }]
description = "Sage Intacct integration for ERPNext — masters in, stock postings out."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

# Frappe Cloud reads this to decide which bench versions the app may be added to.
# ERPNext is required, not optional: the masters sync writes Item, Warehouse and UOM.
[tool.bench.frappe-dependencies]
frappe = ">=16.0.0,<17.0.0"
erpnext = ">=16.0.0,<17.0.0"

[tool.flit.module]
name = "fuse_manufacturing"

[tool.ruff]
line-length = 110
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]
# Frappe apps indent with tabs; E101/W191 fight that convention, not real defects.
ignore = ["E101", "E501", "W191"]

[tool.ruff.format]
indent-style = "tab"
