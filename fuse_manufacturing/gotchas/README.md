# Fuse_Manufacturing gotchas

Same format as `CoWork_Helper/gotchas`. Capture anything non-obvious before closing a session.

Filename: `YYYY-MM-DD-short-title.md`

```
# <what bit us>

**Symptom:** what it looked like
**Cause:** what was actually happening
**Fix:** the working solution
**Applies to:** version / site / component
```
